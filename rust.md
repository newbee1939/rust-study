# Rust 学習メモ

> 📚 参考: https://zenn.dev/mebiusbox/books/22d4c1ed9b0003/viewer/6d5875

---

## 📖 Rustってどんな言語？

**Rust = 「安全で速いプログラムを書ける」言語**

### 🎯 3つの特徴

| 特徴 | 説明 | メリット |
|------|------|----------|
| **静的型付け** | 変数の型を実行前にチェック | typoなどのミスを事前に防げる |
| **式ベース** | `if` やブロック `{}` など、多くの構文が値を返せる | コードがシンプルに書ける |
| **多様なスタイル** | 手続き型や関数型など、複数のスタイルで書ける | 柔軟な書き方ができる |

---

## 💡 式ベース（expression-based）とは

### 要点：Rustでは「式」と「文」を区別する

コードは**「文」**と**「式」**の2種類があります。

| 種類 | 説明 | 例 | 値は返す？ |
|------|------|------|-----------|
| **文（statement）** | 命令を実行する | `let x = 5;` | ❌ 返さない |
| **式（expression）** | 計算して値を返す | `5 + 3` | ✅ 返す（8） |

### 🔹 `if` が値を返せる

**他の言語との違い**：Rustでは `if` 自体が値を返す式として使えます。

```rust
// ❌ 他の言語（JavaScriptなど）ではこう書く
let x;
if (condition) {
    x = 5;
} else {
    x = 10;
}

// ✅ Rustではこう書ける（シンプル！）
let x = if condition { 5 } else { 10 };
// condition が true なら x = 5、false なら x = 10
```

### 🔹 セミコロン `;` の有無で意味が変わる

**重要**：セミコロンをつけるかつけないかで「値を返すか返さないか」が変わります。

```rust
// セミコロンなし → 値を返す
let a = { 5 + 3 };   // a は 8（値を返す）

// セミコロンあり → 値を返さない
let b = { 5 + 3; };  // b は ()（何もない値、ユニット型）

// 関数の戻り値も同じルール
fn add(x: i32, y: i32) -> i32 {
    x + y  // ✅ セミコロンなし！これが戻り値になる
    // x + y;  // ❌ セミコロンをつけると () を返す（エラーになる）
}
```

> 💡 **覚え方**: セミコロン `;` は「ここで終わり！」という意味。つけると値が捨てられる。

---

## 📦 ブロック `{}`

### 要点：ブロックは最後の式の値を返す

`{}` で囲んだ部分を**ブロック**と呼びます。**最後の式の値**がブロック全体の値になります。

```rust
let a = { 5 };      // a は 5（最後の式が 5）
let b = {};         // b は ()（何もない値）

// 複数の処理をまとめられる
let c = {
    let x = 10;
    let y = 20;
    x + y  // 最後の式がブロックの値になる → c は 30
};
```

**使いどころ**:
- ✅ 変数のスコープを限定したいとき
- ✅ 複数の処理をまとめたいとき: `{ 処理1; 処理2; 最後の式 }`

---

## 🎁 オブジェクト

### 要点：Rustではすべての値がオブジェクト

Rustでは**すべての値がオブジェクト**（型を持った値）です。数値 `1` も、文字列 `"hello"` も、関数も、すべてオブジェクトです。

```rust
let a = 1;       // 1 は数値（i32型）のオブジェクト
let b = { 1 };   // ブロック { 1 } も 1 を返す → a と b は同じ値
```

---

## ⭐️ 所有権（Rustで一番大事な概念！）

### 🎯 要点：メモリの安全をコンパイル時に保証する仕組み

所有権は Rust 最大の特徴です。**「メモリの管理をコンパイル時に自動チェックして、安全に使えるようにする仕組み」**です。

**なぜ重要？** → ガベージコレクタなしで、メモリリークや二重解放などのバグを**コンパイル時に**防げます！

---

### 📚 たとえ話：リュックサック

| 操作 | Rustの概念 | 説明 |
|------|-----------|------|
| リュックを友達に**あげる** | **ムーブ（移動）** | あなたはもう使えない |
| リュックを友達に**ちょっと貸す** | **借用（参照 `&`）** | 返してもらえばまた使える |
| 「中身を入れ替えていいよ」と貸す | **可変借用（`&mut`）** | 変更OKで貸す |

---

### 📋 3つの基本ルール

| # | ルール | かんたんに言うと | なぜ？ |
|---|--------|------------------|--------|
| 1️⃣ | すべての値には**所有者が1つだけ** | リュックの持ち主は常に1人 | 誰が片付けるか明確にするため |
| 2️⃣ | 所有者がスコープを抜けると、値は**自動で片付けられる** | 持ち主がいなくなったら自動でゴミ箱行き | メモリリークを防ぐため |
| 3️⃣ | 所有権は**移動（ムーブ）できる** | 持ち主を別の人に変えられる | 値を別の変数に渡せるようにするため |

---

### 🔄 ムーブ（移動）

**要点**：所有権が別の変数に移ると、元の変数は使えなくなります。

```rust
let s1 = String::from("hello");  // s1 が "hello" の所有者
let s2 = s1;                     // 所有権が s1 → s2 に移動！

// println!("{}", s1);  // ❌ エラー！s1はもう使えない（所有権がない）
println!("{}", s2);     // ✅ OK！s2が今の所有者
```

**なぜムーブが必要？** → 同じメモリを2回片付けようとするバグ（二重解放）をコンパイル時に防ぐため。

**他の言語との違い**：
- ❌ C/C++：手動でメモリ管理 → バグが起きやすい
- ✅ Rust：コンパイラが自動チェック → 安全！

---

### 📋 例外：自動的にコピーされる型

**要点**：数値のような**サイズが小さくて単純な型**は、ムーブではなく**コピー**されます。

```rust
let x = 5;
let y = x;  // x の値がコピーされて y に入る（所有権は移動しない）

println!("{}", x);  // ✅ OK（x も使える）
println!("{}", y);  // ✅ OK（y も使える）
```

**コピーされる型（Copyトレイトを実装）**:
- `i32`（整数）
- `f64`（小数）
- `bool`（真偽値）
- `char`（文字）
- タプル（要素がすべてCopy型の場合）

**コピーされない型**:
- `String`（文字列）
- `Vec`（ベクター）
- その他ヒープにデータを持つ型

> 💡 **なぜ String はコピーされないの？**
> → String はヒープにデータを持っていて、サイズが大きくなる可能性があるため。
> → 明示的に `.clone()` を呼ばない限りコピーされません（パフォーマンスのため）。

---

### 🔍 借用（参照）：所有権を渡さずに貸す

**要点**：所有権を手放さずに一時的にデータを見せたいときは **`&`（参照）** を使います。

```rust
// 関数の定義：&String = 「Stringへの参照」を受け取る
fn print_length(s: &String) {
    println!("長さ: {}", s.len());
    // 関数が終わると、参照は返される（所有権は移動しない）
}

let s = String::from("hello");
print_length(&s);  // &s = 「sの参照（読み取り専用の貸し出し）」を渡す
println!("{}", s);  // ✅ まだ使える！所有権は s のまま
```

**メリット**：
- ✅ 所有権を渡さないので、元の変数が使える
- ✅ 関数に渡しても、呼び出し元で値を使い続けられる

---

### ✏️ 可変参照：貸したデータを変更してもらう

**要点**：**`&mut`** をつけると「変更してもいいよ」という参照になります。

```rust
// 関数の定義：&mut String = 「変更してもいい参照」
fn add_world(s: &mut String) {
    s.push_str(" world");  // 文字列を追加できる
}

let mut s = String::from("hello");  // mut をつけて変更可能にする
add_world(&mut s);                  // 変更可能な参照を渡す
println!("{}", s);                  // "hello world" と表示される
```

**大事な制限**: 可変参照は**同時に1つしか作れません**（データ競合を防ぐため）。

```rust
let mut s = String::from("hello");
let r1 = &mut s;  // ✅ OK
// let r2 = &mut s;  // ❌ エラー！同時に2つは作れない
```

**なぜ？** → 複数の場所から同時に変更されると、データが壊れる可能性があるため。

---

### 📊 所有権のまとめ

| 概念 | かんたんな説明 | たとえ | コード例 |
|------|---------------|--------|----------|
| **所有権** | 値の「持ち主」。常に1つだけ | リュックの持ち主 | `let s = String::from("hello");` |
| **ムーブ** | 所有権が別の変数に移る。元の変数は使えなくなる | リュックをあげる | `let s2 = s1;` |
| **コピー** | 小さな型は自動で複製される。両方使える | リュックの中身をコピー | `let y = x;`（xは数値） |
| **借用 (`&`)** | 読み取り専用で一時的に貸す | 「見せて」と借りる | `fn f(s: &String)` |
| **可変借用 (`&mut`)** | 変更OKで一時的に貸す。同時に1つだけ | 「使っていいよ」と貸す | `fn f(s: &mut String)` |

---

### 🎓 所有権の重要性

> **所有権のおかげで、Rustはガベージコレクタなしで安全なメモリ管理を実現しています。**
>
> 最初は難しく感じますが、慣れると「コンパイラが守ってくれている安心感」が分かるようになります！
>
> **メリット**：
> - ✅ メモリリークを防げる
> - ✅ 二重解放を防げる
> - ✅ データ競合を防げる
> - ✅ 実行時のオーバーヘッドが少ない（ガベージコレクタ不要）
