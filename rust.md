## 概要

> 参考: https://zenn.dev/mebiusbox/books/22d4c1ed9b0003/viewer/6d5875

### Rustってどんな言語？

Rustは「安全で速いプログラムを書ける」ことを目指したプログラミング言語です。

- **静的型付け** … 変数の型を実行前にチェックして、typoなどのミスを事前に防げる
- **式ベース** … `if` やブロック `{}` など、多くの構文が値を返せる
- **多様なスタイル** … 手続き型や関数型など、複数のスタイルで書ける

---

## 式ベース（expression-based）とは

コードは**「文」**と**「式」**の2種類があります。

| 種類 | 説明 | 例 |
|------|------|------|
| **文（statement）** | 命令。値は返さない | `let x = 5;` |
| **式（expression）** | 計算して値を返す | `5 + 3` → 8 |

### `if` が値を返せる

Rustでは `if` 自体が値を返す式として使えます。

```rust
let x = if condition { 5 } else { 10 };
// condition が true なら x = 5、false なら x = 10
```

### セミコロン `;` の有無で意味が変わる

セミコロンをつけるかつけないかで「値を返すか返さないか」が変わります。

```rust
let a = { 5 + 3 };   // a は 8（値を返す）
let b = { 5 + 3; };  // b は ()（値を返さない）

fn add(x: i32, y: i32) -> i32 {
    x + y  // セミコロンなし！これが戻り値になる
}
```

> **覚え方**: セミコロン `;` は「ここで終わり！」という意味。つけると値が捨てられる。

---

## ブロック `{}`

`{}` で囲んだ部分を**ブロック**と呼びます。**最後の式の値**がブロック全体の値になります。

```rust
let a = { 5 };      // a は 5
let b = {};         // b は ()（何もない値）
```

**使いどころ**:
- 変数のスコープを限定したいとき
- 複数の処理をまとめたいとき: `{ 処理1; 処理2; 最後の式 }`

---

## オブジェクト

Rustでは**すべての値がオブジェクト**（型を持った値）です。数値 `1` も、文字列 `"hello"` も、関数も、すべてオブジェクトです。

```rust
let a = 1;       // 1 は数値（i32型）のオブジェクト
let b = { 1 };   // ブロック { 1 } も 1 を返す → a と b は同じ値
```

---

## ⭐️ 所有権（Rustで一番大事な概念！）

所有権は Rust 最大の特徴です。**「メモリの管理をコンパイル時に自動チェックして、安全に使えるようにする仕組み」**です。

### たとえ話：リュックサック

- リュックサック（＝値）の持ち主はあなた1人
- リュックを友達に**あげる** → **ムーブ（移動）**（あなたはもう使えない）
- リュックを友達に**ちょっと貸す** → **借用（参照）**（返してもらえばまた使える）
- 友達に「中身を入れ替えていいよ」と貸す → **可変借用**

### 3つの基本ルール

| # | ルール | かんたんに言うと |
|---|--------|------------------|
| 1 | すべての値には**所有者が1つだけ** | リュックの持ち主は常に1人 |
| 2 | 所有者がスコープを抜けると、値は**自動で片付けられる** | 持ち主がいなくなったら自動でゴミ箱行き |
| 3 | 所有権は**移動（ムーブ）できる** | 持ち主を別の人に変えられる |

### ムーブ（移動）

```rust
let s1 = String::from("hello");  // s1 が "hello" の所有者
let s2 = s1;                     // 所有権が s1 → s2 に移動！

// println!("{}", s1);  // ❌ エラー！s1はもう使えない
println!("{}", s2);     // ✅ OK！s2が今の所有者
```

**なぜ？** → 同じメモリを2回片付けようとするバグ（二重解放）をコンパイル時に防ぐため。

### 例外：自動的にコピーされる型

数値のような**サイズが小さくて単純な型**は、ムーブではなく**コピー**されます。

```rust
let x = 5;
let y = x;  // x の値がコピーされて y に入る

println!("{}", x);  // ✅ OK（x も使える）
println!("{}", y);  // ✅ OK（y も使える）
```

**コピーされる型**: `i32`（整数）, `f64`（小数）, `bool`（真偽値）, `char`（文字）など

> **なぜ String はコピーされないの？** → String はヒープにデータを持っていて、サイズが大きくなる可能性があるため。明示的に `.clone()` を呼ばない限りコピーされません。

### 借用（参照）：所有権を渡さずに貸す

所有権を手放さずに一時的にデータを見せたいときは **`&`（参照）** を使います。

```rust
fn print_length(s: &String) {  // &String = 「Stringへの参照」を受け取る
    println!("長さ: {}", s.len());
}

let s = String::from("hello");
print_length(&s);  // &s = 「sの参照（読み取り専用の貸し出し）」を渡す
println!("{}", s);  // ✅ まだ使える！所有権は s のまま
```

### 可変参照：貸したデータを変更してもらう

**`&mut`** をつけると「変更してもいいよ」という参照になります。

```rust
fn add_world(s: &mut String) {  // &mut = 「変更してもいい参照」
    s.push_str(" world");
}

let mut s = String::from("hello");  // mut をつけて変更可能にする
add_world(&mut s);                  // 変更可能な参照を渡す
println!("{}", s);                  // "hello world"
```

**大事な制限**: 可変参照は**同時に1つしか作れません**（データ競合を防ぐため）。

### 所有権のまとめ

| 概念 | かんたんな説明 | たとえ |
|------|---------------|--------|
| 所有権 | 値の「持ち主」。常に1つだけ | リュックの持ち主 |
| ムーブ | 所有権が別の変数に移る。元の変数は使えなくなる | リュックをあげる |
| コピー | 小さな型は自動で複製される。両方使える | リュックの中身をコピー |
| 借用 (`&`) | 読み取り専用で一時的に貸す | 「見せて」と借りる |
| 可変借用 (`&mut`) | 変更OKで一時的に貸す。同時に1つだけ | 「使っていいよ」と貸す |

> **所有権のおかげで、Rustはガベージコレクタなしで安全なメモリ管理を実現しています。** 最初は難しく感じますが、慣れると「コンパイラが守ってくれている安心感」が分かるようになります！
